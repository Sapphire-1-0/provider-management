spring:
  application:
    name: provider-management
  cloud:
    config:
      enabled: false
  jmx:
    enabled: false
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
